# GitHub Actions 构建配置。
# BUILD_ENABLED=false 时，workflow 会跳过构建。
BUILD_ENABLED=true

# CREATE_RELEASE=true 时，workflow 会创建 GitHub Release。
# 默认关闭，避免每次 push 都发布。
CREATE_RELEASE=true

# Android ABI 列表，使用英文逗号分隔。
# 默认只构建 arm64。
TARGET_ABIS=arm64-v8a
